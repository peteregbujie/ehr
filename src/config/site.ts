import { SiteConfig } from "@/types";

const site_url = "/"

export const siteConfig: SiteConfig = {
    name: "EHR",
    description:
        "An Electronic Health Record built with NextJS, Drizzle ORM, Postgres, Tailwind CSS and SHadCN UI",
    url: site_url,

};